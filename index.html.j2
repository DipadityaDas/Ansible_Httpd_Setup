<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Web Page</title>
    <style>
        body {
            background-color: {% if ansible_host == 'node1.lab.com' %}blue{% elif ansible_host == 'node2.lab.com' %}yellow{% else %}white{% endif %};            
            color: {% if ansible_host == 'node1.lab.com' %}white{% else %}black{% endif %};
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Hello from {{ ansible_host }}</h1>
        <h1>This is a sample web page generated with Ansible.</h1>
    </div>
</body>
</html>
